/**
 * Name of RoutesBuilder key which stores its constructor.
 */
export const routesBuilderSpecies = Symbol("routesBuilderSpecies");

/**
 * Name of the special route object field for wildcard route.
 */
export const wildcardRouteKey = Symbol("wildcardRouteKey");
